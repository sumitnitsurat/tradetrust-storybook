import { Meta, Preview, Props, Description, Story } from "@storybook/addon-docs/blocks";
import { Counter } from "./Counter";

<Meta title="MDX|Counter" component={Counter} />

# Counter component

<Description of={Counter} />

# Props

<Props of={Counter} />

# Usage

With `MDX` we can define a story for `Counter` right in the middle of our
markdown documentation.

<Preview withSource>
  <Story name="basic example">
    <Counter />
  </Story>
</Preview>

We can also add more detailed usage on our component. For instance this awesome counter accept an initial value

<Preview>
  <Story name="with initial value">
    <Counter initialValue={10} />
  </Story>
</Preview>
